<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="helloandroid.qushi.com.mylistviewdome">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ViewPagerActivity.ViewPagerActivity" />
        <activity android:name=".ListViewActivity.ListViewActivity" />
        <activity android:name=".NewNavigationActivity.NewNavigationActivity" />
        <activity android:name=".RecyclerViewActivity.RecyclerViewActivity" />
        <activity android:name=".UpLoad.UpLoadActivity" />
        <activity android:name=".UpLoad.ChoseFileToolActivity"></activity>
        <activity android:name=".TestProtocol.TestProtocolAcitvity"></activity>
    </application>

</manifest>